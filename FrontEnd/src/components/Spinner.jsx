function Spinner() {

    const COMPONENT = (
        <div className="loadingSpinnerContainer">
            <div className="loadingSpinner"></div>
        </div>
    )

    return COMPONENT
}

export default Spinner

// EOF
